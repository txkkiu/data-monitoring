Dependencies:
    External Dependencies
        mongodb, python, and pip.
        
    Run `pip install -r requirements.txt`.

Setup:
    Go into main.py and set your sendgrid api key, your source email address (associated with the sendgrid account) and the destination email address.

Running:
    Run `python main.py` and type 'help'

